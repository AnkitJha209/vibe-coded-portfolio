import React from "react";

export default async function vim() {
    return (
        <div className="text-sm">
            Opening vim...
            <div>Tip: To exit vim, type :q!</div>
            <div>Actually, you're already trapped. This is your life now.</div>
        </div>
    );
}
