import React from "react";

export default async function matrix() {
    return (
        <div className="text-sm">
            Matrix rain animation (3s) — enjoy the green cascade.
        </div>
    );
}
